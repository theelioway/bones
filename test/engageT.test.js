const should = require("chai").should()
const mockDb = require("./mockDB.js")
const mockRibs = require("./mockRibs.js")

describe("engageT", () => {
  it("engageT", () => {
    // let mock = { identifier:1, mainEntityOfPage: Person }
    // mockRibs.takesupT(mock)
    true.should.be.true
  })

})
